#include <iostream>
using namespace std;
int main() {
    int n,k;cin>>n>>k;
    int arr[n];
    for(int i=0;i<n;i++)cin>>arr[i];
    int cnt=0;
    int a=arr[k-1];
    for(int i=0;i<n;i++){
        if(arr[i]>0&&arr[i]>=a)cnt++;
    }
    cout<<cnt<<endl;
    return 0;
}
